# Default ProGuard rules. Most are handled by AGP defaults; this file is for
# project-specific tweaks. Kotlinx Serialization rules are pulled in via the
# kotlinx-serialization plugin metadata, so no manual @Keep needed for our
# @Serializable data classes.
